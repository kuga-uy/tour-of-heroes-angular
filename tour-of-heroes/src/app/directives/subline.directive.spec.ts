import { SublineDirective } from './underline.directive';

describe('SublineDirective', () => {
  it('should create an instance', () => {
    const directive = new SublineDirective();
    expect(directive).toBeTruthy();
  });
});
